$kind: http-request
name: Get Desk
url: 'http://localhost:4000/desks/{{deskId}}'
method: GET
order: 3000
headers:
  - key: X-API-Key
    value: demo-key
pathVariables:
  - key: deskId
    value: desk-1
    description: The ID of the desk to fetch
