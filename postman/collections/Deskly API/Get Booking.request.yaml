$kind: http-request
name: Get Booking
url: 'http://localhost:4000/bookings/{{bookingId}}'
method: GET
order: 6000
headers:
  - key: X-API-Key
    value: demo-key
pathVariables:
  - key: bookingId
    value: booking-1
    description: The ID of the booking to fetch
